apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mc-player-service.fullname" . }}-test-connection"
  labels:
    {{- include "mc-player-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mc-player-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
