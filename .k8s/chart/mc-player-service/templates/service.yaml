apiVersion: v1
kind: Service
metadata:
  name: {{ include "mc-player-service.fullname" . }}
  labels:
    {{- include "mc-player-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mc-player-service.selectorLabels" . | nindent 4 }}
